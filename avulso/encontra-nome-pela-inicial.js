const inicial = 'v';

const lista = ['humberto','catarina','valdenio','felipe','vilma','tiago','ana','valter'];

lista.forEach(element => {
    if (element[0] === inicial){
        console.log(element);
    }
});